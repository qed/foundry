# Cross-Cutting & Admin (Phases 105–120, 124–126, 112)

> Platform-wide features: comments system, @mentions, knowledge graph, organization console, real-time presence, collaborative editing, audit trail, project archive.
> Key files: `app/api/projects/[projectId]/activity-log/`, `lib/activity/`, `lib/realtime/`, `components/presence/`, `components/mentions/`, `lib/mentions/`, `app/org/[orgSlug]/settings/`

**105** — Comments System Foundation Schema — SQL migration 009_comments_schema.sql creating polymorphic comments table supporting all entity types idea/feature_node/requirement_doc/blueprint/work_order/feedback via entity_type CHECK constraint and entity_id UUID reference; threaded replies via self-referencing parent_comment_id with CASCADE delete; soft delete via deleted_at timestamp; resolution tracking via is_resolved boolean on root comments; anchored comments via anchor_data JSONB for inline text selection references; RLS policies using is_project_member() for tenant isolation; comment_project_member() SECURITY DEFINER helper; 6 indexes including composite project+entity_type+entity_id; auto-update trigger on updated_at; updated TypeScript types with convenience aliases Comment/CommentEntityType.

**106** — @Mentions System — SQL migration 022_mentions_schema.sql creating mention_references table with project_id FK, comment_id FK to comments with CASCADE delete, mentioned_type CHECK constraint for user/requirement_doc/blueprint/work_order/artifact, mentioned_id UUID, mentioned_name VARCHAR(255); RLS via is_project_member() for SELECT/INSERT/DELETE; 4 indexes on project/comment/mentioned_type+id/created_at DESC; lib/mentions/types.ts with MentionType/MentionMatch/ParsedMention interfaces and MENTION_TYPE_LABELS display map; lib/mentions/parse.ts with parseMentions() regex extraction of @[name](type:uuid) format, buildMentionString() constructor, hasMentions() check; GET /api/projects/[projectId]/mentions/search endpoint searching project members by display_name, requirement_docs/blueprints/work_orders by title, artifacts by name with ILIKE query filtering, results grouped by type with users prioritized; MentionDropdown component with grouped results by type header, keyboard arrow/Enter/Tab/Escape navigation, scroll-into-view on selected, click-outside dismiss, loading state; MentionChip inline component with type-specific colors accent-cyan for users, accent-purple for docs/blueprints, accent-warning for work orders, text-tertiary for artifacts, lucide icons User/FileText/BookOpen/ClipboardList/Paperclip, truncated name display; MentionInput textarea wrapper detecting @ trigger at word boundary, debounced 200ms search, dropdown positioning below textarea, selection inserting @[name](type:id) format with cursor repositioning; RenderMentions component parsing mention syntax from content and rendering interleaved text fragments and MentionChip components; CommentForm updated with projectId prop and MentionInput replacing plain textarea; CommentThread updated with RenderMentions for comment and reply content display, MentionInput for reply form; comments POST API enhanced to parse mentions from content and bulk-insert mention_references rows; updated TypeScript types with MentionReference alias and MentionType.

**110** — Knowledge Graph Explorer Panel — GET /api/projects/[projectId]/connections endpoint with mode=explore returning enriched outbound/inbound connections with resolved entity names from ideas/feature_nodes/blueprints/work_orders/feedback_submissions/artifacts tables, mode=count returning total/outbound/inbound counts and by_type breakdown; POST endpoint creating entity_connections with sourceType/targetType/sourceId/targetId/connectionType validation, duplicate detection via unique constraint (23505), self-connection prevention; DELETE /api/projects/[projectId]/connections/[connectionId] for removing connections; KnowledgeGraphExplorer right-side panel component (w-80 lg:w-96) with header showing total count, search input filtering by entity name or type label with match count display, outbound/inbound sections with ArrowUpRight/ArrowDownLeft direction icons, connections grouped by type using predefined TYPE_ORDER with collapsible groups via ChevronDown/ChevronRight toggle, each connection item showing EntityTypeIcon + clickable name + type label + Sparkles indicator for auto-detected + Trash2 delete button (edit mode only, opacity-0 group-hover); ConnectionTypeIcon component mapping 7 connection types to lucide icons with distinct colors: implements=CheckCircle2 accent-success, depends_on=ArrowDown accent-cyan, references=Link2 accent-purple, relates_to=Network accent-warning, derived_from=GitFork text-tertiary, conflicts_with=AlertTriangle accent-error, complements=PlusCircle accent-cyan; EntityTypeIcon mapping 6 entity types: idea=Lightbulb accent-warning, feature=Layers accent-cyan, blueprint=BookOpen accent-purple, work_order=ClipboardList accent-success, feedback=MessageSquare accent-error, artifact=Paperclip text-secondary; ConnectionCountBadge compact button with Network icon and count, color-coded text-tertiary/accent-cyan/accent-warning/accent-error by count thresholds 0/1-3/4-10/10+; empty state with search icon and guidance text; loading state with Spinner.

**109** — Knowledge Graph Schema — SQL migration 010_knowledge_graph_schema.sql creating entity_connections table for cross-module relationship tracking between ideas/features/blueprints/work_orders/feedback/artifacts with 7 connection types references/depends_on/relates_to/implements/derived_from/conflicts_with/complements; unique constraint preventing duplicate connections; is_auto_detected boolean with JSONB metadata for confidence scores and evidence; bidirectional_connections view mirroring symmetric types relates_to/conflicts_with/complements for both-direction queries; filtered index on manual connections for common lookup patterns; RLS using is_project_member(); 6 indexes plus filtered manual lookup; updated TypeScript types with convenience aliases EntityConnection/EntityConnectionType/GraphEntityType.

**113** — Organization Console — full admin settings UI at /org/[slug]/settings accessible only to org admins; SQL migration 011_org_console_schema.sql adding description and avatar_url columns to organizations; PATCH /api/orgs/[orgId] for updating org name/description/avatar, DELETE /api/orgs/[orgId] for founding-admin-only org deletion with cascading cleanup; GET /api/orgs/[orgId]/members for member list with profile enrichment; PATCH /api/orgs/[orgId]/members/[memberId] for role changes with last-admin and self-modification guards, DELETE for member removal; four-tab console: General Settings with name/description editing and slug display, Members tab with search/role filter/role change dropdown/remove with confirmation dialog, Projects tab with project list and open links, Danger Zone with type-slug-to-confirm deletion dialog; server component with admin role check redirecting non-admins; glass-panel styling with accent-error border on danger zone; updated TypeScript types adding description and avatar_url to organizations.

**117** — Real-Time Presence — Supabase Realtime presence tracking per project channel; usePresence hook subscribing to project:{id}:presence channel with automatic join/leave/sync events and module tracking; useCurrentModule hook deriving active module from URL pathname; presence data includes user_id, display_name, avatar_url, current_module; OnlineIndicator component with green/gray dot and optional label; OnlineUsersList with overlapping avatar stack and overflow counter; PresenceSidebar showing users grouped by module with avatar rows; PresenceWidget integrated into sidebar footer with expandable detail panel and collapsed badge mode; HeaderPresence in header bar showing avatars of users in the same module; presence updates on module navigation without channel re-subscription; automatic cleanup on disconnect via Supabase Realtime.

**118** — Real-Time Collaborative Editing — Yjs CRDT-based collaborative editing infrastructure using Supabase Realtime broadcast channels; lib/collaboration/supabase-yjs-provider.ts custom SupabaseYjsProvider class managing Y.Doc sync via Supabase Realtime broadcast with base64-encoded Yjs updates, separate awareness events for cursor positions, connect/disconnect/destroy lifecycle, onAwarenessChange subscription, getRemoteStates() filtering out local user; lib/collaboration/colors.ts deterministic getUserColor() mapping user IDs to 10-color palette via character code hashing; lib/collaboration/use-collaboration.ts React hook creating Y.Doc and SupabaseYjsProvider per document with channel name format collab:{projectId}:{documentType}:{documentId}, awareness state with user info and cursor position, remote user tracking via onAwarenessChange subscription, connection polling via setInterval, updateCursor callback, Promise.resolve microtask deferral for setState to satisfy React 19 set-state-in-effect rule; components/editors/remote-users-indicator.tsx toolbar widget showing green/amber connection dot + overlapping colored avatar circles with initials for remote editors, overflow counter for maxDisplay exceeded; RequirementsEditor modified with optional ydoc/provider/remoteUsers/isCollaborative props, useMemo extensions list conditionally adding Collaboration.configure({document: ydoc}) and CollaborationCursor.configure({provider}) with history:false when collaborative, content prop set to undefined when Yjs owns state, RemoteUsersIndicator in toolbar; BlueprintEditor same collaboration integration with Table extensions preserved; globals.css collaboration cursor styles .collaboration-cursor__caret with relative positioning and 2px border-left, .collaboration-cursor__label absolute positioned above caret with font-size 0.625rem and color-coded background; packages added yjs, @tiptap/extension-collaboration@^3.20.0, @tiptap/y-tiptap@^3.0.2, @tiptap/extension-collaboration-cursor@3.0.0, y-prosemirror@^1.2.6.

**120** — Project Archive & Cleanup — SQL migration 023_project_archive.sql adding is_archived boolean (default false), archived_at timestamptz, archived_by UUID FK to auth.users with ON DELETE SET NULL to projects table; 2 indexes idx_projects_archived on org_id+is_archived and idx_projects_archived_at filtered on non-null archived_at; updated types/database.ts projects Row/Insert/Update with 3 new archive columns; POST /api/projects/[projectId]/archive endpoint requiring org admin or project leader role, sets is_archived=true with timestamp and user, logs project_archived activity; POST /api/projects/[projectId]/restore endpoint requiring same permissions, clears archive fields, logs project_restored activity; both return 400 for invalid state transitions; GET /api/orgs/[orgId]/projects updated with optional ?archived=true|false query param filter; ProjectsTab rewritten with Active/Archived sub-tabs showing filtered counts, archive button (accent-warning) on active projects, restore button (accent-success) on archived projects, confirmation dialogs for both actions with Button variant danger/primary, toast feedback, optimistic local state update; ArchivedBanner component with accent-warning/10 background and Archive icon showing "This project is archived and read-only"; ArchivedBadge compact inline badge with accent-warning/15 background and "ARCHIVED" text; Header updated to show ArchivedBadge next to breadcrumbs when project.is_archived; project layout renders ArchivedBanner at top of all archived project pages; ProjectSwitcher separates active and archived projects in dropdown with dimmed archived section header using Archive icon.

**124** — Drift Detection — SQL migration 024_drift_alerts.sql creating drift_alerts table with project_id, blueprint_id, requirement_doc_id, feature_node_id FKs, alert_type CHECK (requirement_changed/code_changed), severity CHECK (low/medium/high) default medium, status CHECK (new/acknowledged/resolved) default new, description TEXT, change_summary TEXT, resolved_at/resolved_by fields; RLS via is_project_member() for all operations; composite index on project_id+status; lib/drift/detect.ts with checkAndCreateDriftAlerts fire-and-forget utility that finds blueprints sharing the same feature_node_id as a changed requirement doc, skips if active alert already exists for the pair, sets severity based on blueprint status (approved/implemented=high, in_review=medium, draft=low); PUT requirements-documents/[docId] endpoint enhanced with drift detection call after successful content updates; GET /api/projects/[projectId]/drift-alerts with status/alertType/severity/blueprintId filters, enriches alerts with blueprint and requirement doc titles, returns counts by status; PATCH /api/projects/[projectId]/drift-alerts/[alertId] for status updates with resolved_at/resolved_by tracking on resolve and clearing on reopen; POST /api/projects/[projectId]/drift-alerts/bulk for batch acknowledge/resolve up to 100 alerts; DriftAlertsPanel component with All/New/Ack/Resolved filter tabs, expandable alert cards with severity badges and type labels, checkbox multi-select with bulk Acknowledge/Resolve actions, navigate-to-blueprint links; DriftAlertBanner component fetching active alert count per blueprint and showing warning banner with review link; RoomHeader enhanced with driftCount prop and AlertTriangle badge button; RoomClient manages driftPanelOpen state and fetches new+acknowledged alert counts, renders DriftAlertsPanel as 320px panel between center and right panels; RoomCenterPanel shows DriftAlertBanner for feature blueprints with feature_node_id; types/database.ts updated with drift_alerts Row/Insert/Update types and DriftAlert/DriftAlertType/DriftSeverity/DriftStatus convenience aliases.

**112** — Manual Entity Linking — GET /api/projects/[projectId]/entity-search endpoint searching across ideas (title), feature_nodes (title), blueprints (title), work_orders (title), feedback_submissions (content), artifacts (name) using ILIKE with parallel queries, exclude_type/exclude_id to skip source entity, types filter, already-linked detection via bidirectional entity_connections lookup returning already_linked boolean, results sorted non-linked first then alphabetical, max 50 limit; LinkEntityDialog component with 3-step flow: Step 1 search with 300ms debounced input, real-time results showing EntityTypeIcon + name + type label + status badge + "Already linked" disabled state; Step 2 connection type selection with smart suggestions per entity pair (TYPE_SUGGESTIONS mapping source→target to ordered connection types), suggested section with accent-cyan highlight on selected + Check icon, "Other" section for less common types, each option showing ConnectionTypeIcon + label + description; Step 3 confirmation showing visual Source → ConnectionType → Target summary with entity icons and type labels; creates connection via POST /connections with 409 duplicate handling; KnowledgeGraphExplorer enhanced with Plus button in header opening LinkEntityDialog (when editable), "Link Entity" button in empty state, entityName prop for dialog context; delete confirmation dialog replacing instant delete with Dialog showing connection type label and target name with "This action cannot be undone" warning, Cancel/Remove buttons with loading state; toast notifications via useToast for link created/removed/failed; ConnectionSection sub-component updated from onDelete to onRequestDelete passing connectionId + entityName + connectionType for confirmation dialog context.

**119** — Audit Trail & Activity Log — SQL migration 012_activity_log_schema.sql creating immutable activity_log table with project_id, user_id, entity_type, entity_id, action, JSONB details, ip_address, user_agent fields; RLS via is_project_member() for SELECT and INSERT only with no UPDATE/DELETE policies; 5 indexes including composite project+created_at DESC and project+user+created_at; logActivity() server utility in lib/activity/logging.ts using service client with fire-and-forget error handling; formatAction/formatEntityType/getActionColor display utilities in lib/activity/utils.ts mapping 30+ action types to human-readable labels; GET /api/projects/[projectId]/activity-log endpoint with pagination, filtering by user/action/entity_type/entity_id/date_range/search, profile enrichment; ActivityLogViewer component with debounced filter changes and load-more pagination; ActivityFilters with search input, entity type dropdown, action category dropdown, date range pickers; ActivityItem showing user avatar, action label with color coding, entity type badge, timestamp, optional change summary; activity-log page at /org/[orgSlug]/project/[projectId]/activity-log; updated TypeScript types with ActivityLog alias.
