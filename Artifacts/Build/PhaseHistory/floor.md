# Assembly Floor (Phases 061–080, 127–130)

> Work order management module: schema, layout, work order CRUD, detail view, kanban board with drag-and-drop, table view, phase management, agent chat, comments & activity, leader dashboard, MCP API, phase burndown charts.
> Key files: `components/floor/`, `app/api/projects/[projectId]/work-orders/`, `app/api/projects/[projectId]/phases/`, `lib/agent/floor-context.ts`, `app/api/agent/floor/`

**061** — Assembly Floor Database Schema — SQL migration 005_assembly_floor_schema.sql creating 3 tables: phases with project-scoped name uniqueness and planned/active/completed status tracking with position ordering, work_orders with comprehensive fields including title/description/description_json/acceptance_criteria/implementation_plan/implementation_plan_json plus backlog/ready/in_progress/in_review/done status and critical/high/medium/low priority with feature_node linkage and assignee tracking, work_order_activity for immutable audit trail with action and JSONB details; RLS policies on all 3 tables using is_project_member() for direct project tables; new work_order_project_member() SECURITY DEFINER function for indirect project membership checks on activity table; activity table intentionally has no UPDATE or DELETE policies to enforce immutability; 11 indexes including composite project_id+phase_id; CHECK constraints for status and priority enums; auto-update triggers on phases.updated_at and work_orders.updated_at; blueprint_id FK deferred to Phase 046; updated TypeScript types with full Row/Insert/Update definitions and convenience aliases Phase/WorkOrder/WorkOrderActivity/WorkOrderStatus/WorkOrderPriority/PhaseStatus.

**062** — Assembly Floor Page Layout — FloorClient orchestrator managing kanban/table view toggle persisted to localStorage and URL query param with responsive agent panel auto-collapse below 1200px; FloorHeader with module icon, progress badge showing X/Y work orders complete with percentage, view toggle button group Kanban/Table with accent-cyan active states, disabled New Work Order button placeholder, agent panel toggle; PhaseNavigation with horizontal scrollable tabs including All Phases default tab plus individual phase tabs with status color dots planned/active/completed and per-phase progress counts, scroll arrows for overflow, disabled Add Phase placeholder; FloorContent with kanban board view showing 5 status columns Backlog/Ready/In Progress/In Review/Done with count badges and glass-panel work order cards showing title and priority dot, and table view with 7-column layout Title/Status/Priority/Assignee/Phase/Feature/Updated with status badges and priority indicators; FloorRightPanel agent chat placeholder matching Pattern Shop pattern with Bot icon and disabled input; GET /api/projects/[projectId]/work-orders endpoint with optional phaseId filter and project membership validation; GET /api/projects/[projectId]/phases endpoint ordered by position; server component fetches work order status counts for initial stats; scrollbar-hide utility class added to globals.css.

**063** — Create Work Order Manual — POST /api/projects/[projectId]/work-orders endpoint with server-side validation of title 3-255 chars and priority enum, auto-position calculation via MAX sibling position+1, work_order_activity audit log entry on creation; GET /api/projects/[projectId]/members endpoint fetching project_members enriched with profiles table display_name and avatar_url; CreateWorkOrderModal slide-over panel from right edge with 200ms slide-in-right animation, 460px max width, 7 form fields: title with live character count and 3-char minimum validation, description textarea with 5000 char limit, acceptance criteria textarea, priority 4-button selector with color-coded dots for critical/high/medium/low, assignee dropdown populated from project members API, phase dropdown from phases list, linked feature dropdown from flattened feature tree hierarchy; form fetches members and feature nodes on modal open, resets all fields on close, disables inputs during submission, shows inline error messages; FloorHeader New Work Order button enabled with onNewWorkOrder callback; FloorClient wired with createModalOpen state and fetchKey increment pattern triggering data refetch after successful creation.

**064** — Work Order Detail View — GET /api/projects/[projectId]/work-orders/[workOrderId] fetching single work order enriched with creator profile, assignee profile, phase name, and feature node title via parallel queries; PATCH endpoint with field-level change detection for title/status/priority/assignee_id/phase_id/feature_node_id/description/acceptance_criteria/implementation_plan creating work_order_activity entries for each change with from/to details; GET /api/projects/[projectId]/work-orders/[workOrderId]/activity endpoint with profile-enriched activity feed in reverse chronological order; WorkOrderDetail slide-over component at 560px max-width with slide-in-right animation triggered by clicking kanban cards or table rows; inline editable title with pencil icon on hover and Enter/Escape keyboard handling; status and priority inline dropdowns with color-coded options and optimistic updates; assignee dropdown populated from project members with avatar display; phase dropdown from phases list; editable description/acceptance_criteria/implementation_plan text sections with Save/Cancel buttons; acceptance criteria rendered as bullet list stripping markdown markers; activity feed showing formatted action descriptions with user avatars and timeAgo timestamps; comments section placeholder for Phase 078; FloorContent updated with onWorkOrderClick prop wired to kanban cards and table rows; FloorClient managing selectedWorkOrderId state with fetchKey refetch on updates.

**065** — Kanban Board with Drag-and-Drop — KanbanBoard component using @dnd-kit/core with DndContext, PointerSensor with 5px activation constraint, closestCenter collision detection; 5 droppable KanbanColumn components for backlog/ready/in_progress/in_review/done with status-colored dot headers, count badges, and cyan highlight on drag-over; DraggableCard wrapping CardContent showing title with line-clamp-2, priority color dot with label, acceptance criteria count, and assignee avatar placeholder; DragOverlay with ghost card shadow and 1deg rotation; optimistic status moves via optimisticMoves state map with 3-second auto-clear; columns scroll vertically with max-height calc(100vh-220px); FloorClient handleStatusChange callback performing optimistic setWorkOrders state update then PATCH API call with refetch fallback on error; extracted kanban into dedicated kanban-board.tsx from floor-content.tsx which now delegates to KanbanBoard component.

**067** — Work Order List/Table View — WorkOrderTable component in components/floor/work-order-table.tsx with 8-column sortable table: checkbox selection, title, status badge, priority dot, assignee avatar, phase name, feature name, relative timestamp; column header click cycles sort asc/desc/none with ArrowUp/ArrowDown/ChevronsUpDown icons; grouping buttons for none/status/phase/assignee/priority with collapsible group headers showing item counts; row selection with select-all checkbox and cyan highlight for selected rows; enriched data display using MemberInfo and FeatureInfo lookup maps for assignee avatars and phase/feature name resolution; responsive column hiding Phase on <lg and Feature on <xl; FloorContent updated to delegate table view to WorkOrderTable accepting phases/members/features/selectedIds/onSelectionChange props; FloorClient enhanced to fetch project members and feature tree in parallel with feature tree recursive flattening for lookup enrichment.

**069** — Work Order Phases — full phase CRUD management; POST /api/projects/[projectId]/phases endpoint creating phases with auto-calculated position; PATCH/DELETE /api/projects/[projectId]/phases/[phaseId] for updating name/description/status/position and deleting with work order unphasing; PhaseNavigation enhanced from placeholder to interactive: enabled Add Phase button opening create modal with name+description fields, right-click context menu on phase tabs with Rename/Status cycle/Delete options, inline name editing with Enter/Escape/blur handling, delete confirmation dialog showing affected work order count with accent-warning styling; FloorClient wired with phase CRUD callbacks using optimistic updates for rename and status changes with fetchKey refetch fallback; phase selection persisted to URL query param ?phase=id for shareable bookmarkable filtered views.

**066** — Kanban Card Display — enhanced kanban cards with real assignee data, feature tags, and status styling; KanbanBoard props extended with optional members and features arrays, building lookup Maps for O(1) resolution; CardContent enhanced with real assignee avatars using MemberInfo display_name for initials and avatar_url for images (previously hardcoded "?" with undefined src), priority dot with hover tooltip showing label text, feature tag badge in accent-purple/10 for feature-linked work orders showing truncated feature title, status-specific left border accents (accent-cyan for in_progress, accent-purple for in_review, accent-success for done), done cards faded to opacity-80, enhanced hover with shadow-md elevation and 150ms transition, keyboard accessibility with tabIndex=0, role="button", aria-label, and focus-visible outline styling; DragOverlay CardContent receives same memberMap and featureMap for consistent rendering during drag; FloorContent updated to pass members and features arrays to KanbanBoard component.

**070** — Priority & Sequencing — inline priority editing on kanban cards and table rows via reusable PrioritySelector dropdown component in components/floor/priority-selector.tsx with 4 options (Critical/High/Medium/Low) with color-coded dots, outside-click and Escape close, checkmark on current selection; kanban board rewritten from useDraggable to useSortable with SortableContext per column and verticalListSortingStrategy enabling within-column vertical card reorder via drag-and-drop while preserving cross-column status change drag; GripVertical drag handle icon from lucide-react appears on card hover with opacity-0→opacity-100 group-hover transition, cursor-grab/active:cursor-grabbing styling; DragEnd handler distinguishes column drops (status change) from card drops — same column triggers arrayMove with position recalculation at 100-interval gaps, different column triggers status change; priority dot on kanban cards made clickable opening PrioritySelector dropdown positioned absolutely; priority cell in table rows made clickable with same PrioritySelector; PATCH /api/projects/[projectId]/work-orders/reorder-batch endpoint accepting { items: [{ id, position }] } array for atomic batch position updates with project membership validation per item; FloorClient enhanced with handlePriorityChange callback (optimistic update + PATCH with activity logging) and handleReorder callback (optimistic position map update + batch PATCH with fetchKey refetch fallback); FloorContent updated with onPriorityChange and onReorder props passed through to KanbanBoard and WorkOrderTable.

**074** — Agent: Work Order Extraction from Blueprints — POST /api/projects/[projectId]/work-orders/extract endpoint fetching blueprint content from Supabase, recursively converting TipTap/ProseMirror JSON to plain text, sending to Claude Haiku with structured extraction prompt returning JSON array of work orders with title/description/acceptance_criteria/priority/complexity; Levenshtein similarity-based duplicate detection against existing work order titles (>0.8 threshold); POST /api/projects/[projectId]/work-orders/batch-create endpoint creating up to 50 work orders atomically with auto-position calculation and work_order_activity logging tagged source='blueprint_extraction'; ExtractWorkOrdersModal component with 5-step flow: blueprint selection (multi-select with checkboxes showing type and status), AI extraction loading state, review panel with accept/reject checkboxes and inline editing of title/description/criteria/priority per extracted WO plus duplicate warnings with AlertTriangle icon, batch creation progress, success confirmation with count; purple "Extract" button with Sparkles icon in FloorHeader; component key pattern `${i}-${editing}` for clean state reset on edit toggle; max 5 blueprints per extraction request.

**072** — Work Order Search & Filter — debounced 300ms client-side text search across title/description/acceptance_criteria with Search icon input and X clear button in FloorHeader; FilterPanel popover component with 5 collapsible multi-select sections: Status (backlog/ready/in_progress/in_review/done with color dots), Priority (critical/high/medium/low with color dots), Assignee (project members with avatars + Unassigned), Phase (project phases + Unphased), Feature (feature nodes + No feature linked) — each option showing work order count; FilterBadges component rendering removable chips for all active filters with individual X remove and "Clear all"; combined AND filtering: search AND status AND priority AND assignee AND phase AND feature all compose with existing phase navigation single-select and My Work Orders toggle; all filter state persisted to URL query params (?search=, ?status=, ?priority=, ?assignee=, ?phases=, ?feature=) for shareable bookmarkable filtered views restored on page load; filterCounts computed from full work order set; activeFilterCount badge on filter button; outside-click and Escape close on filter panel.

**071** — Progress Tracking & Rollup — GET /api/projects/[projectId]/progress endpoint computing project-level, per-phase, and per-feature progress stats from work_orders without denormalized columns; PhaseNavigation enhanced with thin progress bars under each phase tab and "All Phases" tab using flex-col gap-1 layout with 1px-height bars — bg-accent-cyan fill, bg-accent-success at 100%, bg-bg-tertiary track; FloorHeader progress display enhanced with 80px visual progress bar next to percentage text replacing verbose "work orders complete" label; feature progress rollup computed client-side in FloorClient as Map<string, { done, total }> from unfiltered workOrders array (not phase-filtered) ensuring feature completion reflects full project scope; featureProgress threaded through FloorContent → KanbanBoard → KanbanColumn → SortableCard → CardContent prop chain; kanban cards with linked features show mini purple progress bar (w-10 h-1) plus done/total count next to feature tag badge, green at 100% completion.

**068** — Work Order Assignment — reusable AssigneeSelector dropdown component in components/floor/assignee-selector.tsx with member search (shown at 5+ members), avatar display, "Unassigned" option, outside-click and Escape close, auto-focus; inline assignment on kanban cards via clickable avatar button with dashed "+" circle for unassigned cards opening AssigneeSelector dropdown positioned absolutely relative to card footer, toggle/close callback separation through KanbanBoard→KanbanColumn→DraggableCard→CardContent prop chain with assigneeOpenFor state managed at board level; inline assignment on table rows via clickable assignee cell button with "+ Assign" text for unassigned rows opening same AssigneeSelector component; "My Work Orders" filter toggle in FloorHeader with UserCheck icon, active/inactive styling with accent-cyan highlight and count badge, URL-persisted via ?filter=my-work-orders query param; FloorClient enhanced with useAuth for current user, myWorkOrdersFilter state, filteredWorkOrders useMemo filtering by user.id, myWorkOrdersCount memo, handleAssignmentChange callback with optimistic setWorkOrders update then PATCH API call with fetchKey refetch fallback; FloorContent updated with onAssignmentChange prop passed through to KanbanBoard and WorkOrderTable; GET /api/projects/[projectId]/work-orders enhanced with ?assigned_to query parameter supporting specific user_id or "unassigned" value using Supabase .is() for null filtering.

**073** — Assembly Floor Agent Infrastructure — AI chat panel in Assembly Floor right panel using Anthropic Claude Haiku with streaming responses; lib/agent/floor-context.ts with FLOOR_SYSTEM_PROMPT guiding agent for work order analysis, phase planning, progress insights, and blocked item detection; buildFloorContext injecting work orders with status/priority aggregate counts and per-item details capped at 50, phase progress summaries with done/total counts, and feature tree overview capped at 30; GET/PUT /api/agent/floor for conversation persistence using agent_conversations table with module='assembly_floor'; POST /api/agent/floor/chat streaming endpoint loading work_orders, phases, and feature_nodes in parallel for context injection; FloorRightPanel rewritten from disabled placeholder to live agent chat with clickable example prompts for empty state, message history with user/assistant bubbles, streaming text display, typing indicator, auto-scroll, Shift+Enter multiline input, conversation persistence across sessions; FloorClient updated to pass projectId to agent panel.

**075** — Agent: Phase Planning & Suggestions — POST /api/projects/[projectId]/work-orders/suggest-phases endpoint using Claude Haiku to analyze all work orders and suggest phase groupings; PLANNING_SYSTEM_PROMPT guiding AI to consider feature grouping, inferred dependencies, priority ordering, complexity balance, and related work; returns primary plan + 1-2 alternatives each with phases containing name, work_order_titles (resolved to IDs server-side via title-to-ID mapping), reasoning, estimated_duration, dependencies, and risks; supports optional constraints (min_phases, max_phases, focus_feature_id, instructions); includes existing phase context so suggestions work with or extend current phases; POST /api/projects/[projectId]/phases/apply-plan endpoint batch-creating phases with auto-position calculation and assigning work orders to phases with work_order_activity logging tagged source='agent_phase_plan'; SuggestPhasePlanModal component with multi-step flow: analyzing (spinner with WO count) → review (primary/alternative plan switcher tabs, overall reasoning display, stats bar showing phase count and assignment coverage, collapsible PhaseCard components with inline-editable names via Enter/Escape/blur, work order lists with priority dots and remove buttons, add-work-order dropdown from unassigned pool, phase reasoning and risk badges, estimated duration, delete phase button, add phase button, unassigned WO warning panel) → applying (progress spinner) → done (success confirmation with phase and WO counts); error handling with retry; FloorHeader enhanced with Wand2 "Plan" button in accent-purple next to Extract button; FloorClient wired with suggestPhasesModalOpen state and fetchKey refetch on apply.

**076** — Work Order Implementation Plans — POST /api/projects/[projectId]/work-orders/[workOrderId]/generate-plan endpoint using Claude Haiku to generate structured markdown implementation plans from work order context (title, description, acceptance criteria, linked feature, phase); PLAN_GENERATION_PROMPT producing Objective, Dependencies, File Changes, Implementation Steps, Key Considerations sections; activity logging as implementation_plan_generated with source='agent'; WorkOrderDetail enhanced implementation plan section replacing EditableSection with custom layout: Wand2 "Generate" button calling AI endpoint with Loader2 spinner during generation; generated plan preview mode with accent-purple border and "AI Generated — Review before accepting" badge; Accept button saves plan directly, "Edit First" button loads generated text into monospace textarea editor for refinement before saving, Reject button discards; MarkdownContent renderer component handling h1-h3 headings, bullet and numbered lists with nested indent support, fenced code blocks with monospace pre/code, inline code with accent-cyan styling, **bold** and *italic* via InlineMarkdown regex parser; monospace font-mono textarea editor with markdown placeholder; activity feed formatAction extended with implementation_plan_generated action type.

**077** — Work Order Bulk Operations — PATCH /api/projects/[projectId]/work-orders/bulk-update endpoint accepting work_order_ids array (max 200) and updates object with optional status/priority/assignee_id/phase_id fields; creates work_order_activity entries per work order per changed field tagged source='bulk_update'; POST /api/projects/[projectId]/work-orders/bulk-delete endpoint deleting work_order_activity entries first (FK cleanup) then work_orders, returns deleted_count and deleted_ids; BulkActionBar component in components/floor/bulk-action-bar.tsx appearing when tableSelectedIds.size > 0 with accent-cyan selection count badge, ActionDropdown reusable component for Status (5 options with color dots), Priority (4 options with color dots), Assign (project members with Avatar + Unassigned), Phase (project phases + Unphased); destructive Delete button in accent-error with Dialog confirmation modal showing AlertTriangle icon, count, and permanent deletion warning; Loader2 spinner during operations; Clear button to deselect; FloorClient enhanced with 5 bulk handlers (handleBulkStatusChange/PriorityChange/Assign/PhaseChange/Delete) each performing optimistic setWorkOrders update then API call with fetchKey fallback and selection clear on success; BulkActionBar rendered between FilterBadges and PhaseNavigation.

**078** — Floor Comments & Activity — Tabbed Comments & Activity section in work order detail slide-over replacing separate Activity section and disabled Comments placeholder; two tabs with accent-cyan active underline and live count badges: Comments (N) and Activity (N); Comments tab reuses existing polymorphic comments infrastructure from Phase 105 schema and Phase 044 API (GET/POST /api/projects/[projectId]/comments with entityType=work_order, PATCH/DELETE per comment, POST resolve toggle); imports CommentThread and CommentForm components from components/shop/ for consistent cross-module UX; full comment CRUD: create with Ctrl+Enter, threaded replies, resolve/unresolve toggle, author-only delete with confirmation; comments fetched in parallel with work order, activity, and members data via Promise.all; refetchComments callback for optimistic refresh after mutations; Activity tab preserves existing ActivityItem rendering with work_order_activity entries; formatAction extended with 'commented' action type; all state (comments, discussionTab) reset on panel close.

**079** — Leader Progress Dashboard — GET /api/projects/[projectId]/dashboard endpoint aggregating project-level progress (total/completed/percentage), status distribution counts across 5 statuses, per-phase progress with name/status/total/completed/percentage, feature tree with parent_id for client-side tree building and rollup percentages, last 20 work_order_activity entries enriched with user profiles and WO titles (fetched via work order IDs since activity table has no project_id), team workload per assignee with assigned/completed/in_progress counts, and blockers list of high/critical priority items in backlog/ready; DashboardClient component in components/dashboard/ with 7 sections: ProgressRing (160px SVG circle with stroke-dasharray animation and color-coded ring), StatusDistribution (stacked horizontal bar + legend), BlockersWidget (CRIT/HIGH tagged items list), PhaseCard grid (glass-panel cards with progress bars and status badges), FeatureTree (collapsible tree with buildTree function aggregating child progress into parents via recursive traverse, inline progress bars), TeamWorkload (stacked bars per member showing done/active/remaining segments), ActivityFeed (last 20 entries with user/action/WO title and timeAgo); integrated into existing project home page below module cards; all read-only; responsive 3-column grid on desktop collapsing to single column on mobile.


**128** — Sprint/Phase Burndown — GET /api/projects/[projectId]/phases/[phaseId]/burndown endpoint computing burndown data from work orders and work_order_activity table; returns phase info, total/completed/remaining counts, statusBreakdown (backlog/ready/in_progress/in_review/done), 7-day rolling velocity (completions per day), estimatedCompletion date extrapolated from velocity, and day-by-day dataPoints array with date/remaining/ideal fields; builds completion history from activity log status transitions to 'done' with fallback to work order updated_at; ideal trend line calculated as linear slope from total at phase start to 0 at current day; PhaseBurndown right-side panel component (w-80 lg:w-96) with header showing phase name and close button; 4-card metrics grid: Total (Target icon cyan), Completed with percentage (CheckCircle2 success), Remaining (Clock warning), Velocity 7d avg with TrendingUp/Minus/TrendingDown indicator; estimated completion info bar with Calendar icon and days-to-complete; completion celebration bar when done=total; stacked status breakdown bar with 5 colors (success/purple/cyan/warning/gray) and legend labels; Recharts ResponsiveContainer LineChart (h-56) with CartesianGrid (#2e3140), date-formatted XAxis, integer YAxis, dark-themed Tooltip (#1a1d27 bg), Legend, two Line series: "Remaining" (cyan, 2px solid) and "Ideal" (gray, 1px dashed); empty states for no work orders and collecting data (< 2 data points); MetricCard and StatusLabel sub-components; integrated into FloorClient with burndownPhaseId state rendered alongside FloorContent; PhaseNavigation context menu extended with BarChart3 "Burndown" option between Status and Delete; recharts package added.

**129** — MCP Implementation — Extended v1 API with 5 new endpoints: GET /api/v1/projects/[projectId]/blueprints listing blueprints with type/status filters and pagination requiring read:blueprints scope; GET /api/v1/projects/[projectId]/blueprints/[blueprintId] returning full blueprint detail including JSON content plus plain text extraction via recursive extractPlainText helper; GET /api/v1/projects/[projectId]/features listing feature tree nodes with read:features scope; GET /api/v1/projects/[projectId]/phases listing phases with read:phases scope; GET /api/v1/projects/[projectId]/requirements listing/searching requirements documents with full-text search via ?query= param using ilike on title+content, feature_node_id filter, and pagination requiring read:requirements scope; all endpoints follow existing v1 pattern with authenticateApiKey, hasScope, checkRateLimit, rateLimitHeaders; added read:blueprints and read:requirements to VALID_SCOPES in lib/mcp/keys.ts; McpConnectionManager dialog component with list/create/key-display view modes, lists active connections showing name/key-preview/agent-type/scope-count/last-used/created-at with revoke button, create form with name input, description, 4 agent type radio cards (code_assistant/ci_cd/github_action/custom), scope checkboxes organized in Read/Write/Admin groups with default read scopes pre-selected, one-time API key display with eye/eye-off visibility toggle and copy-to-clipboard button, warning banner about key not shown again, usage config example for Claude Code/Cursor; Key icon button added to FloorHeader; connectionsOpen state wired in FloorClient.

**127** — Extraction Strategy Config — SQL migration 019_extraction_strategy.sql adding extraction_strategy TEXT column with CHECK constraint (feature-slice/specialist/custom) defaulting to feature-slice, extraction_instructions TEXT, and extraction_strategy_updated_at TIMESTAMPTZ to projects table; GET/PATCH /api/projects/[projectId]/settings/extraction-strategy endpoint with project membership check on GET and leader-only restriction on PATCH, validates strategy enum and requires non-empty instructions for custom strategy with 1500 char max, sanitizes HTML tags from instructions, activity logging via logActivity fire-and-forget; ExtractionStrategyConfig dialog component with 3 radio buttons (Feature-Slice with Box icon, Specialist Split with Boxes icon, Custom with Wrench icon) each showing label and description, conditional instructions textarea appearing when custom selected with character counter and validation warning, fetches current config on open and tracks dirty state for save button enablement, toast notifications on save success/error; updated extraction API route to read project extraction_strategy and extraction_instructions from DB, buildExtractionPrompt function composing BASE_EXTRACTION_PROMPT with strategy-specific addendums (feature-slice: one WO per feature covering all aspects, specialist: split by FE/BE/DB/QA with title prefixes, custom: appends user instructions); Settings2 gear icon button added to FloorHeader between view toggle and AI actions; strategyConfigOpen state wired in FloorClient; ExtractionStrategy type alias exported from types/database.ts.

**130** — WO Sync Alerts — SQL migration 028_wo_sync_alerts.sql adding source_blueprint_id UUID column (REFERENCES blueprints ON DELETE SET NULL) to work_orders table with partial index, creating wo_sync_alerts table with id, project_id, work_order_id, blueprint_id, change_type CHECK (content_changed/requirements_changed/acceptance_criteria_changed), change_summary TEXT, status CHECK (new/acknowledged/resolved), timestamps, resolved_at, resolved_by; RLS via is_project_member() for SELECT/INSERT/UPDATE; auto-update trigger for updated_at; batch-create route updated to persist source_blueprint_id from extraction; blueprint PATCH endpoint extended with fire-and-forget sync alert detection: queries work_orders by source_blueprint_id excluding done status, uses calculateBlueprintChangeSize with 5% threshold, generates change summary via generateBlueprintChangeSummary, batch inserts wo_sync_alerts for all affected WOs; GET /api/projects/[projectId]/work-orders/[woId]/sync-alerts listing alerts with optional status filter, enriched with blueprint titles via separate query; PATCH .../sync-alerts/[alertId] updating status to acknowledged/resolved with resolved_at/resolved_by on resolve; POST .../sync-alerts/bulk-update for multi-select acknowledge/resolve with alert_ids array; SyncAlertsBanner component following drift-alert-banner pattern, fetches new+acknowledged counts via dual API calls, renders warning-styled button with AlertTriangle icon and "Blueprint updated. X sync alerts to review." text; SyncAlertsPanel slide-over with alerts grouped by blueprint_id, select-all checkbox for unresolved, individual checkboxes, bulk action bar (Acknowledge/Resolve), AlertCard sub-component with status-colored borders (warning for new, cyan for acknowledged, muted for resolved), change type labels, change summary, timeAgo timestamps, per-card Acknowledge/Resolve buttons; wired into work-order-detail.tsx with syncAlertsPanelOpen state, banner renders after metadata grid when source_blueprint_id exists, panel renders at z-60; WoSyncAlert/WoSyncAlertChangeType/WoSyncAlertStatus type aliases added to types/database.ts.

**080** — MCP Connection Schema & API — SQL migration 021_mcp_connections.sql creating mcp_connections table with id, project_id, name, description, api_key_hash (SHA-256 for deterministic DB lookup), api_key_preview (last 8 chars), agent_type CHECK (code_assistant/ci_cd/github_action/custom), status CHECK (active/revoked), rate_limit default 100, scopes TEXT[] array, last_used_at, created_by, revoked_at, revoked_by; RLS policies via is_project_member() for SELECT/INSERT/UPDATE; lib/mcp/keys.ts with generateApiKey (fnd_ prefix + 32 random hex bytes), hashApiKey (SHA-256), VALID_SCOPES array (read:work-orders, write:status, write:assignment, write:priority, read:features, read:phases, write:create-work-orders, admin:project), hasScope with admin:project wildcard; lib/mcp/auth.ts authenticateApiKey extracting Bearer token, hashing, querying DB for active connection, fire-and-forget last_used_at update; lib/mcp/rate-limit.ts in-memory sliding window (1min) with checkRateLimit returning allowed/remaining/resetTime and rateLimitHeaders helper; POST/GET /api/projects/[projectId]/mcp/connections for create (returns raw key once) and list (preview only); POST .../[connectionId]/revoke setting status=revoked with revoked_at/revoked_by; v1 API routes with API key auth: GET/POST /api/v1/projects/[projectId]/work-orders (list with status/assignee filter, pagination via range, create with auto-position), GET /api/v1/projects/[projectId]/work-orders/[workOrderId] (detail + activity), PATCH .../status (scope write:status), PATCH .../assign (scope write:assignment), PATCH .../priority (scope write:priority), POST /api/v1/auth/validate; all v1 endpoints enforce project isolation, scope validation, rate limiting with X-RateLimit-* headers, activity logging with source='mcp_api' and connection_name; McpConnection type added to types/database.ts.
