# Insights Lab (Phases 081–095)

> Feedback collection and analysis module: schema, layout, inbox display with sorting/pagination, detail view.
> Key files: `components/lab/`, `app/api/projects/[projectId]/feedback/`, `app/api/projects/[projectId]/app-keys/`

**081** — Insights Lab Database Schema — SQL migration 006_insights_lab_schema.sql creating 2 tables: app_keys for project-scoped API keys with active/revoked status and unique key_value constraint for external feedback collection authentication, feedback_submissions with content text and category classification bug/feature_request/ux_issue/performance/other/uncategorized plus new/triaged/converted/archived status tracking and optional AI priority score 0-100, text array tags, JSONB metadata for browser/device/page_url/user_agent, conversion tracking via converted_to_work_order_id FK to work_orders and converted_to_feature_id FK to feature_nodes, nullable submitter_email and submitter_name; RLS policies using is_project_member() for tenant isolation plus special insert policy allowing external submissions with valid app keys via is_active_app_key() SECURITY DEFINER helper; 7 indexes including composite project+created_at DESC for inbox listing, project+status for filtering, project+score DESC NULLS LAST for prioritization; auto-update trigger on feedback_submissions.updated_at; updated TypeScript types with full Row/Insert/Update definitions and convenience aliases AppKey/FeedbackSubmission/AppKeyStatus/FeedbackCategory/FeedbackStatus.

**083** — Insights Lab Page Layout — two-panel layout with LabClient orchestrator managing feedback inbox 40% width and detail panel 60% width; LabHeader with module icon, color-coded stat badges for Total/New/Triaged/Converted feedback counts, refresh button with spin animation, agent panel toggle with accent-purple active state; LabInbox showing feedback list with content preview line-clamp-2, status dots cyan/warning/success/tertiary for new/triaged/converted/archived, category badges with color coding bug-error/feature_request-purple/ux_issue-warning/performance-cyan, submitter name, timeAgo timestamps, selected state with accent-cyan border; LabDetailPanel with status and category badges, full content with whitespace-pre-wrap, tags as rounded pills, submitter metadata, placeholder conversion action buttons for Phase 088/089; LabAgentPanel as fixed overlay sidebar with slide-in translate-x transition, backdrop overlay with click-to-close, X close button, disabled chat input for Phase 090; GET /api/projects/[projectId]/feedback endpoint with status/category/search filters using typed FeedbackStatus/FeedbackCategory casting for Supabase queries; server component fetches feedback status counts for initial render stats; responsive with detail panel hidden on mobile.

**084** — Feedback Inbox Display — enhanced inbox list view with sorting, pagination, and priority scoring; GET /feedback API updated with sort parameter supporting newest/oldest/highest_score, limit/offset pagination with total count via Supabase count:'exact', secondary sort on created_at for score ties; LabInbox component enhanced with FeedbackInboxHeader section showing total count badge and sort dropdown with 3 options, FeedbackInboxItem extracted component with status dot pulse animation via animate-ping for new items, category badge with color coding, priority score display with Gauge icon, submitter name/email fallback, relative timestamps with full date hover tooltips; InboxPagination section with prev/next buttons and current page indicator at 20 items per page; LabClient updated with sort and page state management, PAGE_SIZE constant, query parameter construction for API calls, handleSortChange resetting page to 1; FeedbackSort type exported from lab-inbox for cross-component use.

**085** — Feedback Detail View — completed (no detailed description recorded in the phase history paragraph).

**082** — Feedback Collection API — public-facing POST /api/insights/feedback endpoint for collecting user feedback from deployed applications; X-App-Key header authentication looking up app_keys table by key_value with active status check; input validation for content 10-5000 chars with sanitization (null byte removal, trim, length enforcement), optional email format validation, submitter name max 255 chars, metadata max 50KB with viewport width/height positive number check; rate limiting at 100 requests per minute per app key reusing sliding window from lib/mcp/rate-limit.ts with X-RateLimit-* response headers and Retry-After on 429; CORS support with Access-Control-Allow-Origin * on all responses plus OPTIONS preflight handler with 24h max-age; auto-captures browser name (Chrome/Firefox/Safari/Edge), device type (mobile/tablet/desktop), page URL from Referer header via lib/feedback/browser-utils.ts; content sanitization via lib/feedback/validation.ts; inserts into feedback_submissions with project_id from app key, category uncategorized, status new; returns 201 with id/project_id/category/status/created_at on success.

**086** — Feedback Categorization — reusable CategoryBadge component in components/lab/category-badge.tsx with Lucide icons per category (AlertCircle bug, Lightbulb feature_request, Eye ux_issue, Zap performance, Tag other, HelpCircle uncategorized) and brand-consistent colors (accent-error, accent-success, accent-purple, accent-warning, text-tertiary), 3 sizes sm/md/lg with rounded-full styling; enhanced category selector in lab-detail-panel.tsx header showing CategoryBadge in trigger button and dropdown options with Check icon on current selection; tag chips restyled with accent-purple/10 background; tag input enhanced with autocomplete suggestions fetched from GET /api/projects/[projectId]/feedback/tags endpoint that aggregates unique tags across all project feedback; tag validation enforcing min 2 chars max 50 chars with hint text, case-insensitive duplicate prevention, Escape key to close suggestions; inbox list items in lab-inbox.tsx now use CategoryBadge component with icons replacing plain text category labels; exported CATEGORY_CONFIG for reuse in other components; bulk categorization deferred to Phase 095.

**088** — Convert Feedback → Work Order — ConvertToWoModal component in components/lab/convert-to-wo-modal.tsx with pre-filled form: title extracted from first line/sentence of feedback content via extractTitle(), description built as Markdown template with original feedback content plus context metadata (submitter, browser, device, page, timestamp, viewport) via buildDescription(), priority auto-set from feedback category (bug/performance→high, feature_request/ux_issue→medium, other→low) via getPriorityFromCategory(); assignee dropdown populated from GET /api/projects/[projectId]/members returning display_name; POST /api/projects/[projectId]/feedback/[feedbackId]/convert-to-wo endpoint atomically creates work order with backlog status and auto-calculated position, updates feedback.converted_to_work_order_id and sets feedback status to converted, logs work_order_activity with source feedback_conversion; prevents double conversion with 409 status; detail panel Convert to Work Order button now live (no longer disabled), shows Converted to work order badge after conversion; Escape key closes modal; success toast with work order title; Convert to Feature button still disabled pending Phase 089.

**090** — Insights Lab Agent Infrastructure — lib/agent/lab-context.ts with buildLabContext() aggregating feedback by category/status/tags and buildLabSystemPrompt() injecting feedback summaries into system prompt; GET/PUT /api/agent/lab for conversation history persistence in agent_conversations table with module 'insights_lab'; POST /api/agent/lab/chat streaming endpoint using Anthropic Claude Haiku (claude-haiku-4-5-20251001) with 1500 max_tokens, loads up to 200 feedback items for context, keeps last 20 conversation messages, streams text deltas via ReadableStream; components/lab/lab-agent-panel.tsx fully replaced from disabled stub to functional chat UI with message history, streaming display with bouncing dots indicator, auto-scroll to bottom, conversation persistence across page visits via GET/PUT API, Escape key to close; reuses shared AgentMessage and AgentInput components from components/hall/; lab-client.tsx updated to pass projectId to agent panel; agent can analyze feedback patterns, suggest categorizations, detect duplicates, recommend priorities, and suggest conversions to work orders or features.

**087** — Feedback Search & Filter — extended GET /api/projects/[projectId]/feedback endpoint with multi-select filtering: comma-separated status and category params using Supabase `.in()`, tags param using `.overlaps()` for OR-logic matching, dateFrom/dateTo range filters with end-of-day padding on dateTo, scoreMin/scoreMax range with 0-100 defaults; FeedbackFilterBar component in components/lab/feedback-filter-bar.tsx (~530 lines) with debounced search input (300ms via setTimeout/useRef), collapsible filter panel toggled by button with active filter count badge, filter chips showing active filters with individual remove and clear-all; MultiSelectDropdown sub-component with select-all/deselect-all, checkmark indicators, outside-click-to-close; DateRangeDropdown with 4 presets (24h/7d/30d/90d) plus custom date inputs and clear button; ScoreRangeInput with dual range sliders and debounced updates (500ms); URL persistence via useSearchParams() + router.replace() syncing filter state to query string for shareable/bookmarkable filtered views; parseFiltersFromURL() and filtersToURLParams() helpers in lab-client.tsx; countActiveFilters() for badge display; FeedbackFilters interface exported with search/categories/statuses/tags/dateFrom/dateTo/scoreMin/scoreMax fields; Lab page wrapped in Suspense boundary required by useSearchParams(); all filter changes reset pagination to page 1; styled with brand colors (bg-primary, border-default, accent-cyan, text-tertiary).

**094** — App Key Management — settings page at /org/[orgSlug]/project/[projectId]/settings/insights with full API key lifecycle management; migration 029_app_key_management.sql adding environment TEXT CHECK (production/staging/development/custom), description TEXT, revoked_at TIMESTAMPTZ, revoked_by UUID to app_keys table with project+environment index; GET /api/projects/[projectId]/app-keys returns keys with masked key_preview (first 8 + last 4 chars), POST creates key with hf_ prefix + 28 random hex chars stored as plaintext key_value, PATCH revokes with revoked_at/revoked_by tracking, DELETE enforces 7-day post-revocation guard; AppKeySettings orchestrator in components/lab/app-key-settings.tsx with table displaying name/description, masked key code block, environment badges color-coded (production=error, staging=warning, development=cyan, custom=gray), Active/Revoked status with Check/Lock icons, timeAgo timestamps, revoke button for active keys, delete button for 7-day-old revoked keys; CreateKeyDialog two-step flow: form step with name input (required, max 100), environment select, description textarea (optional, max 500) → created step showing full key in accent-cyan monospace with copy button, warning banner about one-time visibility; 5-second key reveal via toggleReveal with auto-hide setTimeout; revoke and delete confirmation dialogs using Dialog component with warning/error banners; SetupInstructions component with JavaScript/cURL tabbed code examples, copy button, required/optional field documentation; Settings link added to sidebar.tsx after Artifacts with Settings icon and "API keys & config" description.

**091** — Agent Auto-Categorization — automatic AI-driven feedback categorization using Claude Haiku (claude-haiku-4-5-20251001) triggered 2 seconds after feedback submission via fire-and-forget setTimeout in app/api/insights/feedback/route.ts; lib/agent/categorize-feedback.ts contains categorization logic that loads the feedback item, fetches up to 10 similar previously categorized items for context, builds system prompt with category guidelines (bug/feature_request/ux_issue/performance/other) and priority scoring factors, parses JSON response for category, confidence (0-100), reasoning, suggestedTags (1-5), score (0-100), scoreReasoning; validates and clamps all values before updating feedback_submissions with ai_suggested=true and categorization_reasoning as JSON string containing confidence/reasoning/scoreReasoning; idempotency guard via .eq('category', 'uncategorized') on update; migration 030_auto_categorization.sql adds ai_suggested BOOLEAN DEFAULT false and categorization_reasoning TEXT to feedback_submissions; POST /api/projects/[projectId]/feedback/[feedbackId]/categorize endpoint for manual re-categorization (resets to uncategorized then re-runs agent); PATCH endpoint enhanced to support score, ai_suggested, categorization_reasoning fields, auto-clears ai_suggested when user manually changes category; lab-detail-panel.tsx updated with purple Sparkles AI badge showing confidence percentage in header, RotateCcw re-categorize button with spin animation during processing, AI Analysis section below priority score bar showing confidence level (High/Medium/Low) and reasoning text, scoreReasoning displayed under priority bar; lab-inbox.tsx updated with purple Sparkles icon on AI-categorized items next to CategoryBadge.

**092** — Agent Feedback Enrichment — AI-powered feedback enrichment using Claude Haiku (claude-haiku-4-5-20251001); migration 031_feedback_enrichment.sql adding enrichment JSONB column to feedback_submissions with partial index on project_id where enrichment IS NOT NULL; lib/agent/enrich-feedback.ts core enrichment function that fetches target feedback, up to 100 other feedback items in the project for comparison, and up to 50 feature_nodes (using title/description/level), builds system prompt requesting JSON with summary (1-2 sentences), keyIssues (3-5 bullets), affectedComponents (string[]), suggestedFeatures (matched by exact ID from provided list with matchScore 0-100 and matchReason), relatedFeedback (matched by exact ID with similarity 0-100, content previews stored from lookup), duplicateRisk (isDuplicate boolean for >90 similarity, confidence, relatedFeedbackIds for >80); JSON parsed via regex /\{[\s\S]*\}/, all fields validated and clamped, stored via JSON.parse(JSON.stringify(enrichment)); POST /api/projects/[projectId]/feedback/[feedbackId]/enrich endpoint verifying project membership and feedback ownership, calls enrichFeedback(), returns updated feedback object; PATCH endpoint enhanced with enrichment field support; components/lab/feedback-enrichment.tsx FeedbackEnrichmentSection component with two modes: unenriched shows "Enrich with AI" button (accent-cyan styling, pulse animation during processing), enriched shows results in accent-cyan/5 bordered panel with collapsible sections via expandedSection state toggle — duplicate risk warning banner (amber accent with AlertTriangle icon), italicized summary quote, Key Issues with cyan bullet points, Affected Components as accent-purple pills, Related Features with name + match score badges + match reasons, Related Feedback with content previews in bg-tertiary cards + similarity percentages; re-analyze button with RotateCcw spin animation; timeAgo display for enrichedAt timestamp; integrated into lab-detail-panel.tsx between AI Categorization Reasoning and Tags sections.

**095** — Feedback Bulk Operations — multi-select and bulk action capabilities for feedback inbox; POST /api/projects/[projectId]/feedback/bulk endpoint supporting 4 operations: categorize (sets category + clears ai_suggested), add_tags (fetches current tags per item and merges with deduplication), archive (sets status to archived), delete (hard delete); validates project membership, max 100 items per request, verifies all IDs belong to project; components/lab/bulk-actions-bar.tsx floating action bar fixed bottom center z-40 with bg-bg-secondary dark theme, selection count display, categorize dropdown showing all 6 categories with Lucide icons from CATEGORY_CONFIG, tag dropdown with text input + autocomplete suggestions fetched from /feedback/tags endpoint + Plus button + Enter key support, archive button with Dialog confirmation showing warning icon, delete button with danger Dialog confirmation; lab-inbox.tsx enhanced with checkboxes on each FeedbackInboxItem (custom div-based checkbox with accent-cyan checked state and Check icon), select-all checkbox in inbox header with three visual states (unchecked/partial via Minus icon/all-checked via Check icon), selected count badge in header, checked items get subtle bg-accent-cyan/3 highlight; lab-client.tsx updated with selectedIds Set<string> state, handleToggleSelect toggling individual items, handleToggleAll selecting all visible or clearing all, handleClearSelection, handleBulkActionComplete clearing selection and refreshing list; selection clears automatically on filter change, sort change, and page change; keyboard shortcuts via document keydown listener: Cmd/Ctrl+A selects all visible items (skips when INPUT/TEXTAREA/contentEditable focused), Escape clears selection when items selected; selection persists while navigating detail panel; toast notifications on successful bulk operations.

**093** — Agent Conversion Suggestions — AI-powered conversion suggestion engine using Claude Haiku (claude-haiku-4-5-20251001); lib/agent/conversion-suggestions.ts with ConversionSuggestion interface (type work_order/feature/monitor, title, description, priority critical/high/medium/low, effort xs/s/m/l/xl, impact 0-100, feedbackIds, reasoning, suggestedParentFeature), DuplicateGroup interface (title, feedbackIds, similarity), CategoryTrend interface (category, trend increasing/stable/decreasing, thisWeek, lastWeek), ConversionSuggestionsResult aggregating recommendations/duplicateGroups/trends/summary/generatedAt; computeTrends() server-side function comparing last 7 days vs previous 7 days per category using Date arithmetic with +1 threshold for trend direction; generateConversionSuggestions(projectId) loads up to 200 non-archived feedback, 50 feature_nodes, filters unconverted items, builds system prompt for product prioritization expert with max 8 recommendations ranked by impact, effort sizing guidelines (xs <1hr to xl 1+week), duplicate groups requiring 2+ items and >80% similarity; validates all feedbackIds against actual unconverted set, clamps priority/effort/type to valid enums, limits impact to 0-100; POST /api/projects/[projectId]/feedback/conversion-suggestions endpoint with project membership verification; components/lab/conversion-suggestions-panel.tsx full panel UI with three states: initial (Sparkles icon + Generate Suggestions button), loading (spinner + "Analyzing feedback patterns..."), results (summary, category trends with TrendingUp/Minus/TrendingDown icons and color coding increasing-error/stable-tertiary/decreasing-success, recommendations list with type icons FileText/GitBranch/Eye and colors cyan/purple/tertiary, priority badges with PRIORITY_STYLES critical-error/high-warning/medium-cyan/low-tertiary, effort and impact badges, feedback count, suggestedParentFeature link, reasoning text, per-item convert buttons for work_order and feature types, converting state tracked via convertingIds Set); handleConvert calls existing convert-to-wo or convert-to-feature endpoints with suggestion data; handleConvertAll sequentially converts all actionable (non-monitor) recommendations; duplicate groups section with amber warning styling showing similarity percentages; Refresh button and Convert all ({count}) in header; lab-header.tsx updated with Sparkles "Suggestions" toggle button alongside Agent toggle; lab-client.tsx updated with suggestionsActive state, ConversionSuggestionsPanel conditionally rendered in right panel replacing LabDetailPanel, clicking inbox item auto-switches back to detail view via handleSelectFeedback.

**089** — Convert Feedback → Feature — ConvertToFeatureModal component in components/lab/convert-to-feature-modal.tsx with two-column layout: left side shows searchable expandable feature tree fetched from GET /api/projects/[projectId]/feature-tree with expand/collapse toggles, search filter via nodeMatchesSearch(), level badges (Epic/Feature/Sub-feature/Task), only epic and feature levels selectable via SELECTABLE_LEVELS set, selected node highlighted with accent-cyan border and Check icon, breadcrumb path display via getNodePath(), "Clear selection (create as Epic)" deselect button; right side has pre-filled title via extractTitle() from feedback first line/sentence max 120 chars, Markdown description via buildDescription() with ## User Feedback, ## Context (submitter/browser/device/page/received), ## Why This Matters placeholder sections, title validation 3-255 chars, description validation min 10 chars; POST /api/projects/[projectId]/feedback/[feedbackId]/convert-to-feature endpoint verifies project membership, prevents double conversion with 409 on existing converted_to_feature_id, auto-determines child level from parent via CHILD_LEVELS map (epic→feature, feature→sub_feature, sub_feature→task, task→null rejects), calculates next sibling position, creates feature_node with not_started status, links feedback.converted_to_feature_id and sets feedback status to converted; detail panel Convert to Feature button now live (no longer disabled), shows Converted to feature badge after conversion; purple accent theme throughout modal; root-level creation defaults to epic level when no parent selected.
